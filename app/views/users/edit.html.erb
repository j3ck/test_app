<div class="jumbotron">
<h2>Edit</h2>

<%= form_for(@user) do |f| %>

<%= render 'form', f: f %>

<div class="form-group"><%= f.submit "Update", class: "btn btn-primary" %></div>
<% end %>

<%= link_to "Back", users_url %>
</div>