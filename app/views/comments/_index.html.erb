<div id="new-book-comment-wrapper">
	<%= render "comments/add_comment", :comment => @book.comments.build %>
</div>
<div id="comments">
	<%= render :partial => "comments/comment", :collection => @comments.roots, :as => 'comment' %>
</div>